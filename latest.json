{
  "version": "3.34.1",
  "release_date": "2026-08-06",
  "linux_x64": {
    "url": "https://github.com/VoxyWatch/publish/releases/download/v3.34.1/voxywatch-v3.34.1-linux-x64.tar.gz",
    "sha256": "28c6af3b25a6efa0509f0e3b84c2b1db7aeed226ad614a0a502b1f18eb7fb55b",
    "signature": "https://github.com/VoxyWatch/publish/releases/download/v3.34.1/voxywatch-v3.34.1-linux-x64.tar.gz.asc"
  },
  "linux_arm64": {
    "url": "",
    "sha256": ""
  },
  "changelog": [
    "isolated Internal IP imports",
    "Choosing an Internal IP CSV or JSON file no longer writes immediately. The portal shows the exact filename and a read-only merged-count preview, then requires a",
    "Internal IP and Trunks imports keep independent inputs and state; late reads from an older selection cannot replace the newest file.",
    "After a successful import, the visible IP Directory, its ETag and its in-memory labels refresh through one canonical path instead of leaving stale rows on scree"
  ],
  "min_upgrade_from": "1.0.0",
  "changelog_es": [
    "Internal IP imports now show the selected filename and a read-only preview before requiring an explicit Apply action, preventing a previously selected Trunks fi",
    "Late FileReader or preview responses cannot replace a newer file selection, and the visible IP Directory refreshes immediately after a successful import.",
    "The server supports a no-write Internal IP import preflight and keeps invalid endpoint imports fail-closed."
  ]
}