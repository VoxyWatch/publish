{
  "version": "2.155.2",
  "release_date": "2026-07-01",
  "linux_x64": {
    "url": "https://github.com/VoxyWatch/publish/releases/download/v2.155.2/voxywatch-v2.155.2-linux-x64.tar.gz",
    "sha256": "056113ab652b65cd97ee2cc16add56184bcebb57caf225bb2cfb2ec7f7c54924",
    "signature": "https://github.com/VoxyWatch/publish/releases/download/v2.155.2/voxywatch-v2.155.2-linux-x64.tar.gz.asc"
  },
  "linux_arm64": {
    "url": "",
    "sha256": ""
  },
  "changelog": [
    "C3ntro capture state no longer scans compressed hypertables",
    "Process",
    "The portal no longer uses `SELECT MAX(id), MIN(id) FROM packets` to detect capture changes. On compressed TimescaleDB chunks that query can open hundreds of chu",
    "`getDbState()` now reads the latest `id` from the newest chunk and uses `timescaledb_information.chunks` to detect retention via `minTs`, avoiding a full hypert"
  ],
  "min_upgrade_from": "1.0.0",
  "changelog_es": [
    "C3ntro evita escaneo pesado de estado de captura",
    "Process",
    "El portal ya no usa `SELECT MAX(id), MIN(id) FROM packets` para detectar cambios de captura. En TimescaleDB con chunks comprimidos esa consulta abre cientos de",
    "`getDbState()` ahora lee el último `id` desde el chunk más nuevo y usa `timescaledb_information.chunks` para detectar retención por `minTs`, evitando full-sc"
  ]
}